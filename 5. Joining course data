## The base data file does not include fields related to:
  # 1. Course short title
  # 2. Course full title
  # 3. Semester
  
library(dplyr)
HxMx <- left_join(HXMITX, courses, by = "course_code", copy = FALSE)
View(HMITX)
